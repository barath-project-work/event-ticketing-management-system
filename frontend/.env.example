# Event Ticketing Frontend - Environment Variables
# Copy this file to .env and customize

# Admin API token (must match the backend ADMIN_TOKEN setting)
VITE_ADMIN_TOKEN=admin-token-001

# Backend API URL (only needed if different from Vite proxy target)
# VITE_API_URL=http://localhost:8080
