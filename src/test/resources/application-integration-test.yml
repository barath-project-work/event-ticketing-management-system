spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        generate_statistics: true
        format_sql: true

logging:
  level:
    com.ticketing: DEBUG
    org.testcontainers: INFO
